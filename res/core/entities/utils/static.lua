-- A 3D model and a static 3D collider, wrapper functions so it's written only once

local static = {}

-- Holds the model in the GPU as long as the static is used
static.gpu_model_pointer = nil
static.collider = nil

function static:create(model_path) 


    
end



return static